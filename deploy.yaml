app:
  - script: /server/index.js
    name: 'movie-trailer'
    env: 
      COMMON_VARIABLE: true
    env_production:
      NODE_ENV: production
deploy:
  production:
    user: root
    host:
      - 101.201.237.72
    port: 22
    ref: origin/master
    repo: 
      git@github.com:lhz960904/movie-admin.git
    path:
      /var/www/movie-trailer
    ssh_options: "StrictHostKeyChecking=no"
    pre-deploy: git fetch --all
    post-deploy: 'npm install && npm run build && pm2 startOrRestart deploy.yaml --env production'
    env:
      NODE_ENV: production